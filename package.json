{
  "name": "token-maxxing-harness",
  "version": "0.1.0",
  "description": "Agentic coding harness that min-maxes token spend with a custom model router",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "demo": "tsx src/demo.ts"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "tsx": "^4.23.1",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^4.0.18",
    "ai": "^7.0.35",
    "zod": "^4.4.3"
  }
}
