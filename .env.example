ANTHROPIC_API_KEY=

# Optional: only needed to route some subtasks to a local Ollama model instead of Anthropic.
# OLLAMA_MODEL=llama3.2          # a model you've already `ollama pull`ed
# OLLAMA_BASE_URL=http://localhost:11434/v1   # default; override for a remote/non-default Ollama
